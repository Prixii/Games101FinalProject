#pragma once
#include <utility>
using VertexIndex = size_t;
using HalfEdgeIndex = size_t;
using FaceIndex = size_t;
using VertexPairIndex = size_t;
using VertexPairCostIndex = size_t;

using EdgeKey = std::pair<VertexIndex, VertexIndex>;
